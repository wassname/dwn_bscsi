from setuptools import find_packages, setup

setup(
    name='bscsi_data',
    packages=find_packages(),
    version='0.1.0',
    description='download, preproc, and load optionmetris data from http://b.scsi.to/',
    author='wassname',
    license='proprietary',
)
